<?php

if (isset($_SERVER['REQUEST_URI']) && $_SERVER['REQUEST_URI'] != '/') {
	$options = array(
		'query' => array(
			'return' => $_SERVER['REQUEST_URI']
		)
	);
} else {
	$options = array();
}

?>

<h1>Unauthorised</h1>
<p>
	The requested resource requires authentication. Please
	<a href="<?php echo $this->url('log-in', array(), $options); ?>">log-in</a>
	to continue.
</p>